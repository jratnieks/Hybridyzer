# RunPod GPU Requirements
# Install with: pip install -r requirements_runpod.txt --extra-index-url=https://pypi.nvidia.com

# Core dependencies
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
pyarrow>=10.0.0

# RAPIDS for GPU acceleration (CUDA 12)
# Note: These must be installed from NVIDIA's PyPI index
cudf-cu12>=24.08.0
cuml-cu12>=24.08.0
cupy-cuda12x>=12.0.0

# Optional but recommended
numba>=0.58.0  # For CUDA support
